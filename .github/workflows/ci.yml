name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  NODE_OPTIONS: "--max-old-space-size=8192"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build native dependencies
        run: npm rebuild better-sqlite3

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Test (server)
        run: npx vitest run --project server

      - name: Test (client shard 1/2)
        run: npx vitest run --project client --shard=1/2
        env:
          NODE_OPTIONS: "--max-old-space-size=3072"

      - name: Test (client shard 2/2)
        run: npx vitest run --project client --shard=2/2
        env:
          NODE_OPTIONS: "--max-old-space-size=3072"
